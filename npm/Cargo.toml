[package]
name = "blit"
version = "0.2.2"
edition = "2021"
description = "Low-latency terminal streaming — WASM client"

[lib]
crate-type = ["cdylib"]

[dependencies]
blit-remote = { path = "../remote" }
wasm-bindgen = "0.2"
js-sys = "0.3"
