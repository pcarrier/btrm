#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")/.."
(cd browser && wasm-pack build --target web --release --out-dir ../web)
cargo build --release -p blit-server -p blit-gateway -p blit-demo
echo "built: target/release/blit-server target/release/blit-gateway target/release/chaos"
