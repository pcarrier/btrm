#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")/.."
(cd browser && wasm-pack build --target web --release --out-dir ../web)
cargo build --release -p btrm-server
echo "built: target/release/btrm-server"
