[package]
name = "blit-server"
version = "0.7.0"
edition = "2021"

[dependencies]
blit-remote = { path = "../remote" }
blit-wezterm = { path = "../wezterm-driver" }
tokio = { version = "1", features = ["full"] }
libc = "0.2"
