[package]
name = "blit-server"
version = "0.2.3"
edition = "2021"

[dependencies]
blit-remote = { path = "../remote" }
blit-wezterm = { path = "../wezterm-driver" }
tokio = { version = "1", features = ["full"] }
libc = "0.2"
