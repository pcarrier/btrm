[workspace]
resolver = "2"
members = [
    "remote",
    "wezterm-driver",
    "server",
    "gateway",
    "browser",
    "demo",
    "cli",
    "fonts",
]

[profile.release]
opt-level = "s"
lto = true
