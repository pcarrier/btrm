#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")/.."

# Build WASM module
(cd crates/browser && wasm-pack build --target web --release --out-dir pkg)

# Build React web app
(cd libs/web-app && npm install --prefer-offline && npm run build)
